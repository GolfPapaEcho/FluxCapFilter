use mvc
Open a window called HAL9000 - the main window for the program
Spawns input data  file path capture child window
Wait for user to choose a file
run model
model extracts DATA0_pF time using pandas
model calculates sample frequency
Spawns plots
saves the output csv of time and DATA0_pF

NB Joe the only relavent files are HAL900App.py
view.py controller.py and model.py
